{"id":"mai-trmo","kind":"ticket","type":"task","title":"Auto docs sync on every mai write when docs.sync = auto","priority":1,"tags":["docs","auto-sync"],"body":"When docs.sync is \"auto\" in config, every Create/Append on a kind:doc note auto-materializes its file. No daemon needed.\n\nAfter engine writes a doc note:\n1. Check config.Docs.Sync == \"auto\"\n2. If yes, run SyncDocs for just that note's file (not full sync)\n3. Async, don't block the command\n\nThis covers the agent→disk direction without a daemon.","timestamp":"2026-03-28T20:55:39Z","branch":"main"}
