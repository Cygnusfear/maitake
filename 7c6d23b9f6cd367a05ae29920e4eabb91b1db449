{"id":"mai-dydw","kind":"ticket","type":"task","title":"CI import boundary check for pkg/notes","priority":3,"tags":["architecture","ci","substrate"],"body":"## What\n\nAdd a CI script (or test) that enforces pkg/notes never imports domain packages.\n\n### Check\n```bash\nimports=$(go list -f '{{join .Imports \"\\n\"}}' ./pkg/notes/ | grep -E 'pkg/(crdt|docs|pr|changelog)')\nif [ -n \"$imports\" ]; then\n  echo \"FAIL: pkg/notes imports domain packages: $imports\"\n  exit 1\nfi\n```\n\n### Options\n1. Shell script in `scripts/check-boundary.sh` — run in CI\n2. Go test in `pkg/docs/boundary_test.go` — already exists, but only runs when someone runs tests\n3. Both — belt and suspenders\n\n### Currently\n`pkg/docs/boundary_test.go` has `TestBoundary_NotesDoesNotImportCRDT` which does exactly this via `go list`. But it only runs when `go test ./pkg/docs/` is run. A CI script makes it a hard gate.\n\n### Also check\n- pkg/notes must not import pkg/docs\n- pkg/notes must not import pkg/crdt  \n- cmd/mai must not import pkg/crdt (domain logic goes through pkg/docs)\n\n## Goal\nCI enforces the substrate boundary — violations break the build\n\n## Acceptance Criteria\n- [ ] Script or CI step checks pkg/notes imports\n- [ ] Fails if pkg/notes imports pkg/crdt, pkg/docs, or any domain package\n- [ ] Runs on every push/PR\n\n## Verification\n- [ ] Temporarily add 'import pkg/crdt' to pkg/notes, verify check fails\n- [ ] Remove it, verify check passes","timestamp":"2026-04-04T13:13:37Z","branch":"main"}
