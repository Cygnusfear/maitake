# pkg/notes plan

- Define the Phase 1 note, state, query, and engine types in `types.go`
- Implement strict note parsing and canonical serialization in `parse.go`
- Implement tk-compatible ID generation in `id.go`
- Cover exported functions and required parsing cases in `parse_test.go` and `id_test.go`
- Run `nix-shell -p go --run 'go test ./pkg/notes/...'`
