{"id":"mai-hg02","kind":"ticket","type":"task","title":"You are a HOSTILE test engineer. Your mission: write the most brutal, adversarial tests for the CLI commands that have ZERO test coverage — specifically `lattice.go` (docs sync, check, refs, expand) and `expand.go`.\n\n## Working Directory\n\nRead these files first:\n- `cmd/mai/lattice.go` (291 lines — runDocsSync, runCheck, runRefs, runExpand, matchesRef)\n- `pkg/notes/expand.go` (132 lines — Expand, ExpandRefs, firstParagraph)\n- `pkg/notes/docs.go` (the docs sync engine that lattice.go calls)\n- `test/cli_test.go` (test infrastructure — `mai()`, `maiFail()`, `setupTestRepo()`, `gitRun()`)\n- `test/docs_test.go` (existing docs sync tests — build on these patterns)\n- `test/docs_adversarial_test.go` (existing adversarial docs tests — see what's already covered)\n\n## Output\n\nCreate TWO test files:\n\n### 1. `pkg/notes/expand_test.go`\nUnit tests for the `Expand`, `ExpandRefs`, and `firstParagraph` functions.\n\n### 2. `test/lattice_test.go`  \nCLI integration tests for `mai check`, `mai refs`, `mai expand`, and additional `mai docs sync` edge cases.\n\n## What to test for expand.go — be EVIL\n\n### ExpandRefs\n1. Text with `[[valid-id]]` — resolves to note title/summary\n2. Text with `[[non-existent-id]]` — left as-is or marked unresolved\n3. Text with multiple `[[refs]]` in one string\n4. Text with nested brackets `[[[[nested]]]]`\n5. Text with empty ref `[[]]`\n6. Text with no refs at all — returned unchanged\n7. Text with `[[partial-id]]` — partial match resolution\n8. Ref that matches a closed note vs open note\n\n### Expand (full function)\n9. Expand with context enrichment\n10. Expand on empty string\n11. Expand on string with only whitespace\n\n### firstParagraph\n12. Normal multiline text — returns first paragraph\n13. Single line — returns it\n14. Empty string — returns empty\n15. Text starting with blank lines\n16. Text with only newlines\n\n## What to test for lattice.go — be BRUTAL\n\n### mai check\n17. Check on repo with valid `[[refs]]` in notes — passes\n18. Check on repo with broken `[[refs]]` — reports broken links\n19. Check on repo with `// @mai:` code refs — validates them\n20. Check on repo with broken code refs — reports errors\n21. Check on empty repo (no notes) — clean pass\n22. Check output format — parseable, useful\n\n### mai refs\n23. refs on a note ID — shows what references it\n24. refs on a note that nothing references — empty/clean\n25. refs on a non-existent ID — error or empty\n26. refs on a file path — shows notes targeting that file\n\n### mai expand  \n27. expand with `[[ref]]` text — resolves and prints\n28. expand with no refs — prints unchanged\n29. expand with broken refs — handles gracefully\n\n### mai docs sync edge cases\n30. Docs sync with conflicting file content (edited both in git and in note)\n31. Docs sync with special characters in filenames\n32. Docs sync with deeply nested directory structures\n33. Docs sync with note that has no title (edge case for filename generation)\n34. Docs sync dry-run vs real — verify dry-run doesn't write\n\n### matchesRef (helper function — test via CLI since it's in package main)\n35. refs command with exact ID match\n36. refs command with partial ID match\n37. refs command with path match\n\n## Build and run\n\n```bash\nnix-shell -p go --run 'go test ./pkg/notes/ -count=1 -timeout 60s -run TestExpand -v'\nnix-shell -p go --run 'go test ./test/ -count=1 -timeout 180s -run TestLattice -v'\n```\n\nAlso verify ALL existing tests still pass:\n```bash\nnix-shell -p go --run 'go test ./... -count=1 -timeout 180s'\n```\n\n## Rules\n- Every test must PASS. If you find actual bugs, document with `// BUG:` comment.\n- For expand_test.go: you're in `package notes` — you can access unexported functions.\n- For lattice_test.go: you're in `package test` — use the CLI helpers.\n- Error messages must show got/want.\n- Commit when all tests pass.","assignee":"adversarial-lattice-2","tags":["team"],"body":"You are a HOSTILE test engineer. Your mission: write the most brutal, adversarial tests for the CLI commands that have ZERO test coverage — specifically `lattice.go` (docs sync, check, refs, expand) and `expand.go`.\n\n## Working Directory\n\nRead these files first:\n- `cmd/mai/lattice.go` (291 lines — runDocsSync, runCheck, runRefs, runExpand, matchesRef)\n- `pkg/notes/expand.go` (132 lines — Expand, ExpandRefs, firstParagraph)\n- `pkg/notes/docs.go` (the docs sync engine that lattice.go calls)\n- `test/cli_test.go` (test infrastructure — `mai()`, `maiFail()`, `setupTestRepo()`, `gitRun()`)\n- `test/docs_test.go` (existing docs sync tests — build on these patterns)\n- `test/docs_adversarial_test.go` (existing adversarial docs tests — see what's already covered)\n\n## Output\n\nCreate TWO test files:\n\n### 1. `pkg/notes/expand_test.go`\nUnit tests for the `Expand`, `ExpandRefs`, and `firstParagraph` functions.\n\n### 2. `test/lattice_test.go`  \nCLI integration tests for `mai check`, `mai refs`, `mai expand`, and additional `mai docs sync` edge cases.\n\n## What to test for expand.go — be EVIL\n\n### ExpandRefs\n1. Text with `[[valid-id]]` — resolves to note title/summary\n2. Text with `[[non-existent-id]]` — left as-is or marked unresolved\n3. Text with multiple `[[refs]]` in one string\n4. Text with nested brackets `[[[[nested]]]]`\n5. Text with empty ref `[[]]`\n6. Text with no refs at all — returned unchanged\n7. Text with `[[partial-id]]` — partial match resolution\n8. Ref that matches a closed note vs open note\n\n### Expand (full function)\n9. Expand with context enrichment\n10. Expand on empty string\n11. Expand on string with only whitespace\n\n### firstParagraph\n12. Normal multiline text — returns first paragraph\n13. Single line — returns it\n14. Empty string — returns empty\n15. Text starting with blank lines\n16. Text with only newlines\n\n## What to test for lattice.go — be BRUTAL\n\n### mai check\n17. Check on repo with valid `[[refs]]` in notes — passes\n18. Check on repo with broken `[[refs]]` — reports broken links\n19. Check on repo with `// @mai:` code refs — validates them\n20. Check on repo with broken code refs — reports errors\n21. Check on empty repo (no notes) — clean pass\n22. Check output format — parseable, useful\n\n### mai refs\n23. refs on a note ID — shows what references it\n24. refs on a note that nothing references — empty/clean\n25. refs on a non-existent ID — error or empty\n26. refs on a file path — shows notes targeting that file\n\n### mai expand  \n27. expand with `[[ref]]` text — resolves and prints\n28. expand with no refs — prints unchanged\n29. expand with broken refs — handles gracefully\n\n### mai docs sync edge cases\n30. Docs sync with conflicting file content (edited both in git and in note)\n31. Docs sync with special characters in filenames\n32. Docs sync with deeply nested directory structures\n33. Docs sync with note that has no title (edge case for filename generation)\n34. Docs sync dry-run vs real — verify dry-run doesn't write\n\n### matchesRef (helper function — test via CLI since it's in package main)\n35. refs command with exact ID match\n36. refs command with partial ID match\n37. refs command with path match\n\n## Build and run\n\n```bash\nnix-shell -p go --run 'go test ./pkg/notes/ -count=1 -timeout 60s -run TestExpand -v'\nnix-shell -p go --run 'go test ./test/ -count=1 -timeout 180s -run TestLattice -v'\n```\n\nAlso verify ALL existing tests still pass:\n```bash\nnix-shell -p go --run 'go test ./... -count=1 -timeout 180s'\n```\n\n## Rules\n- Every test must PASS. If you find actual bugs, document with `// BUG:` comment.\n- For expand_test.go: you're in `package notes` — you can access unexported functions.\n- For lattice_test.go: you're in `package test` — use the CLI helpers.\n- Error messages must show got/want.\n- Commit when all tests pass.","timestamp":"2026-03-29T14:15:34Z","branch":"main"}
